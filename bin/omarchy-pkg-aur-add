#!/bin/bash

# Add the named packages from Flatpak (replaces AUR on Ubuntu).

for pkg in "$@"; do
  if ! flatpak list --app 2>/dev/null | grep -qi "$pkg"; then
    flatpak install -y flathub "$pkg" || exit 1
  fi
done

exit 0
