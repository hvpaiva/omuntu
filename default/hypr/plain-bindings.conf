# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omuntu-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, omuntu-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omuntu-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, omuntu-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omuntu-launch-browser --private
bindd = SUPER SHIFT, N, Editor, exec, omuntu-launch-editor

# Add extra bindings
# bindd = SUPER SHIFT, A, ChatGPT, exec, omuntu-launch-webapp "https://chatgpt.com"
# bindd = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Overwrite existing bindings, like putting Omuntu Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omuntu menu, exec, omuntu-menu
