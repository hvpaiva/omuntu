#!/bin/bash

# Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Omuntu defaults and restart the service.

omuntu-refresh-config swayosd/config.toml
omuntu-refresh-config swayosd/style.css
omuntu-restart-swayosd
