#!/bin/bash

# Overwrite the user configs for the Waybar menu bar with the Omuntu defaults and restart the service.

omuntu-refresh-config waybar/config.jsonc
omuntu-refresh-config waybar/style.css
omuntu-restart-waybar
