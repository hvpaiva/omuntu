#!/bin/bash
url="$1"
web_url="https://app.hey.com"

# Handle mailto: URLs
if [[ $url =~ ^mailto: ]]; then
  email="${url#mailto:}"
  web_url="https://app.hey.com/messages/new?to=$email"
fi

exec omuntu-launch-webapp "$web_url"
