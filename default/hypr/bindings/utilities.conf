# Menus
bindd = SUPER, SPACE, Launch apps, exec, omuntu-launch-walker
bindd = SUPER CTRL, E, Emoji picker, exec, omuntu-launch-walker -m symbols
bindd = SUPER CTRL, C, Capture menu, exec, omuntu-menu capture
bindd = SUPER CTRL, O, Toggle menu, exec, omuntu-menu toggle
bindd = SUPER ALT, SPACE, Omuntu menu, exec, omuntu-menu
bindd = SUPER, ESCAPE, System menu, exec, omuntu-menu system
bindld = , XF86PowerOff, Power menu, exec, omuntu-menu system
bindd = SUPER, K, Show key bindings, exec, omuntu-menu-keybindings
bindd = , XF86Calculator, Calculator, exec, gnome-calculator

# Aesthetics
bindd = SUPER SHIFT, SPACE, Toggle top bar, exec, omuntu-toggle-waybar
bindd = SUPER CTRL, SPACE, Theme background menu, exec, omuntu-menu background
bindd = SUPER SHIFT CTRL, SPACE, Theme menu, exec, omuntu-menu theme
bindd = SUPER, BACKSPACE, Toggle window transparency, exec, hyprctl dispatch setprop "address:$(hyprctl activewindow -j | jq -r '.address')" opaque toggle
bindd = SUPER SHIFT, BACKSPACE, Toggle window gaps, exec, omuntu-hyprland-window-gaps-toggle
bindd = SUPER CTRL, BACKSPACE, Toggle single-window square aspect, exec, omuntu-hyprland-window-single-square-aspect-toggle

# Notifications
bindd = SUPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = SUPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = SUPER CTRL, COMMA, Toggle silencing notifications, exec, omuntu-toggle-notification-silencing
bindd = SUPER ALT, COMMA, Invoke last notification, exec, makoctl invoke
bindd = SUPER SHIFT ALT, COMMA, Restore last notification, exec, makoctl restore

# Toggles
bindd = SUPER CTRL, I, Toggle locking on idle, exec, omuntu-toggle-idle
bindd = SUPER CTRL, N, Toggle nightlight, exec, omuntu-toggle-nightlight

# Control Apple Display brightness
bindd = CTRL, F1, Apple Display brightness down, exec, omuntu-brightness-display-apple -5000
bindd = CTRL, F2, Apple Display brightness up, exec, omuntu-brightness-display-apple +5000
bindd = SHIFT CTRL, F2, Apple Display full brightness, exec, omuntu-brightness-display-apple +60000

# Captures
bindd = , PRINT, Screenshot, exec, omuntu-cmd-screenshot
bindd = ALT, PRINT, Screenrecording, exec, omuntu-menu screenrecord
bindd = SUPER, PRINT, Color picker, exec, pkill hyprpicker || hyprpicker -a

# File sharing
bindd = SUPER CTRL, S, Share, exec, omuntu-menu share

# Waybar-less information
bindd = SUPER CTRL ALT, T, Show time, exec, notify-send "    $(date +"%A %H:%M  —  %d %B W%V %Y")"
bindd = SUPER CTRL ALT, B, Show battery remaining, exec, notify-send "$(omuntu-battery-status)"

# Control panels
bindd = SUPER CTRL, A, Audio controls, exec, omuntu-launch-audio
bindd = SUPER CTRL, B, Bluetooth controls, exec, omuntu-launch-bluetooth
bindd = SUPER CTRL, W, Wifi controls, exec, omuntu-launch-wifi
bindd = SUPER CTRL, T, Activity, exec, omuntu-launch-tui btop

# Dictation
bindd = SUPER CTRL, X, Toggle dictation, exec, voxtype record toggle

# Zoom
bindd = SUPER CTRL, Z, Zoom in, exec, hyprctl keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float + 1')
bindd = SUPER CTRL ALT, Z, Reset zoom, exec, hyprctl keyword cursor:zoom_factor 1

# Lock system
bindd = SUPER CTRL, L, Lock system, exec, omuntu-lock-screen
