#!/bin/bash

OMUNTU_PATH=${OMUNTU_PATH:-$HOME/.local/share/omuntu}
branch=$(git -C "$OMUNTU_PATH" branch --show-current 2>/dev/null || echo "unknown")

case "$branch" in
master) echo "stable" ;;
dev) echo "dev" ;;
*) echo "$branch" ;;
esac
